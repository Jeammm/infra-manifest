# Create namespace before SealedSecret (wave 0) and cloudflare-ingress App (wave 1).
apiVersion: v1
kind: Namespace
metadata:
  name: cloudflare-tunnel-ingress-controller
  annotations:
    argocd.argoproj.io/sync-wave: "-1"
