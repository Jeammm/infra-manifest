apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: archery-tracker
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/jeammm/archery-tracker
    targetRevision: main
    path: k8s
  destination:
    server: https://kubernetes.default.svc
    namespace: archery-tracker
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true